name: aizu
services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: aizu-qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage
    environment:
      - QDRANT_API_KEY=${QDRANT_API_KEY:-}
    restart: unless-stopped
  aizu-app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: aizu-app
    ports:
      - "8501:8501"
    depends_on:
      - qdrant
    volumes:
      - .:/app
      - /app/__pycache__
      - /app/.venv
      - /app/.streamlit
    environment:
      - QDRANT_URL=http://qdrant:6333
      - PYTHONUNBUFFERED=1
      - TZ=Asia/Jakarta
      - HF_HOME=/app/hf_cache
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    stdin_open: true
    tty: true

volumes:
  qdrant_storage:
    name: aizu_qdrant_storage

networks:
  default:
    name: aizu-network
    driver: bridge